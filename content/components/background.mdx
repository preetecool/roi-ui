---
title: Background
description:
    A WebGL background component with animated noise shaders and customization options.
links:
    doc: https://thebookofshaders.com/11/
---

<ComponentPreview name="background-demo" />

## Installation

<InstallationTabs>

<InstallationTabsList>
    <InstallationTabsTrigger value="cli">CLI</InstallationTabsTrigger>
    <InstallationTabsTrigger value="manual">Manual</InstallationTabsTrigger>
</InstallationTabsList>

<InstallationTabsContent value="cli">

```package-install
npx shadcn@latest add https://roiui.com/r/background.json
```

</InstallationTabsContent>

<InstallationTabsContent value="manual">

Install dependencies:

```package-install
npm install @base-ui-components/react
```

Copy and paste the TSX file:

<ComponentSource
    src="registry/brook/ui/background/background.tsx"
    title="components/ui/background.tsx"
    collapsible={true}
/>

Copy and paste the CSS file:

<ComponentSource
    src="registry/brook/ui/background/background.module.css"
    title="components/ui/background.module.css"
    language="css"
    collapsible={true}
/>

</InstallationTabsContent>

</InstallationTabs>

## Anatomy

<CodeBlock
filename="anatomy"
  language="tsx"
  code={`import Background from '../ui/background'

<Background />
`} />

## Props

<PropTable
    data={[
        {
            prop: "className",
            type: "string",
        },
        {
            prop: "primaryColor",
            type: "[number, number, number]",
            default: "[0.85, 0.85, 0.85]",
            description: "Primary pattern color in normalized RGB format (values 0-1)",
        },
        {
            prop: "secondaryColor",
            type: "[number, number, number]",
            default: "[0.25, 0.25, 0.25]",
            description: "Secondary pattern color in normalized RGB format (values 0-1)",
        },
        {
            prop: "backgroundColor",
            type: "[number, number, number]",
            default: "[0.0, 0.0, 0.0]",
            description: "Base background color in normalized RGB format (values 0-1)",
        },
        {
            prop: "backgroundOpacity",
            type: "number",
            default: "0.0",
            description: "Background opacity (0=transparent, 1=opaque)",
        },
        {
            prop: "animationSpeed",
            type: "number",
            default: "0.3",
            description: "Overall animation speed multiplier for all moving patterns",
        },
        {
            prop: "noiseScale",
            type: "number",
            default: "3.0",
            description: "Scale of the noise patterns - higher values create more detail",
        },
        {
            prop: "intensity",
            type: "number",
            default: "0.9",
            description:
                "Pattern visibility strength - controls how visible the effects are",
        },
        {
            prop: "octaves",
            type: "number",
            default: "4",
            description:
                "Number of noise octaves for fractal detail - more octaves = more detail",
        },
        {
            prop: "warpStrength",
            type: "number",
            default: "4.0",
            description: "Domain warping intensity - creates organic flowing patterns",
        },
    ]}
/>
